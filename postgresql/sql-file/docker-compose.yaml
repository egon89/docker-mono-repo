services:
  pgdump:
    image: postgres:18
    environment:
      PGHOST: ${PGHOST}
      PGPORT: ${PGPORT}
      PGUSER: ${PGUSER}
      PGPASSWORD: ${PGPASSWORD}
      PGDATABASE: ${PGDATABASE}
    command: ["/bin/bash"]
    network_mode: "host"
    volumes:
      - ./output:/output
